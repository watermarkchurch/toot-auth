require 'spec_helper'

describe Toot::Auth do
  it 'has a version number' do
    expect(Toot::Auth::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
